import { AllowValuesDirective } from './allow-values.directive';

describe('AllowValuesDirective', () => {
  it('should create an instance', () => {
    const directive = new AllowValuesDirective();
    expect(directive).toBeTruthy();
  });
});
